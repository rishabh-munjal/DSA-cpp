/*

You are given a string S of lowercase characters, 
find the rank of the string amongst its permutations when sorted
lexicographically. Return 0 if the characters are repeated in the string.
Note: Return the rank%1000000007 as the answer as rank might overflow.

*/

#include<bits/stdc++.h>
using namespace std;

int findRank(string A , int a){

    int result;

    



    return result;
}
int main(){

    string A;
    int a = A.length();

    cout<<findRank(A,a);

    return 0;
}